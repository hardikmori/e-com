import 'package:flutter/material.dart';

class Explopre extends StatelessWidget {
  const Explopre({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(child: 
    Scaffold(
      body: Center(
        child: Text('exploper'),
      ),
    ));
  }
}